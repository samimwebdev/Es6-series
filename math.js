/* eslint-disable */
const arr = [1, 2, 3];

export const calcultedNumber = arr.reduce((firstNum, eachNum) => {
  return firstNum + eachNum;
}, 0);
export default arr;

// default export
// name export
